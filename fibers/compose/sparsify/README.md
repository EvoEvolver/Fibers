
# What is sparse?

- The number of children of each node is limited
- The length of each child is limited


# Why sparse?

We want to make the tree sparse because we want to put the environment of each node into the prompt. If the tree is not sparse, the prompt will be too long and the performance of LLM will be affected.
